const actions = require_code("actions");
const constants = require_code("constants");
const eventHandlers = require_code("eventHandlers");
const keybinds = require_code("keybinds");
const mage = require_code("mage");
const utils = require_code("utils");

keybinds.setupDefaults();
mage.main();
